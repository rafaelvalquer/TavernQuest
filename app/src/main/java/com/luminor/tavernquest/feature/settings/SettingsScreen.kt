package com.luminor.tavernquest.feature.settings
import androidx.compose.foundation.layout.*;import androidx.compose.material3.*;import androidx.compose.runtime.*;import androidx.compose.ui.Modifier;import androidx.compose.ui.unit.dp;import androidx.hilt.navigation.compose.hiltViewModel
@Composable fun SettingsScreen(onGameReset:()->Unit,vm:SettingsViewModel=hiltViewModel()){val s by vm.ui.collectAsState();var ask by remember{mutableStateOf(false)};var pending by remember{mutableStateOf(0)};Column(Modifier.fillMaxSize().padding(16.dp),verticalArrangement=Arrangement.spacedBy(16.dp)){Text("Configurações");SettingsToggle("Sons",s.sound,vm::sound);SettingsToggle("Vibração",s.haptics,vm::haptics);OutlinedButton({vm.requestLogout({vm.logoutDiscardingPending(onGameReset)},{pending=it})}){Text("Sair da conta")};OutlinedButton({ask=true}){Text("Limpar dados deste aparelho")};AboutSection()};if(ask)ResetGameDialog({vm.reset{onGameReset()}},{ask=false});if(pending>0)androidx.compose.material3.AlertDialog(onDismissRequest={pending=0},title={Text("Atividades aguardando sincronização")},text={Text("Há $pending atividade(s) ainda não enviada(s). Sair agora irá descartá-las deste aparelho.")},confirmButton={androidx.compose.material3.TextButton({vm.logoutDiscardingPending(onGameReset);pending=0}){Text("Sair e descartar")}},dismissButton={androidx.compose.material3.TextButton({pending=0}){Text("Cancelar")}})}
