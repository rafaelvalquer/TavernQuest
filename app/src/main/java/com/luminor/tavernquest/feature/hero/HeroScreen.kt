package com.luminor.tavernquest.feature.hero
import androidx.compose.foundation.layout.*;import androidx.compose.material3.Text;import androidx.compose.runtime.*;import androidx.compose.ui.Modifier;import androidx.compose.ui.unit.dp;import androidx.hilt.navigation.compose.hiltViewModel
@Composable fun HeroScreen(vm:HeroViewModel=hiltViewModel()){val s by vm.ui.collectAsState();Column(Modifier.fillMaxSize().padding(16.dp),verticalArrangement=Arrangement.spacedBy(16.dp)){Text("Herói");s.hero?.let{HeroHeader(it)};s.progress?.let{HeroProgressSection(it)};StreakSection(s.streak);HeroStatisticsSection()}}
