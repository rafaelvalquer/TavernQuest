{
  "name": "tavernquest-firebase-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "npx --yes firebase-tools@15.30.0 emulators:exec --project demo-tavernquest --only firestore,storage \"node --test firestore.rules.test.mjs storage.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
